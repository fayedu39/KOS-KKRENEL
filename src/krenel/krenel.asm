; Tout le KKrenel
; XD LoL PTDR Jamy la p'tite poubelle

%include "krenel\pmode.asm" ; On passe en mode protégé
%include "krenel\ints.asm" ; On a besoin des interruptions juste après, sans ça rien ne marche